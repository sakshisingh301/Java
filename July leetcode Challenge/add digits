

class Solution {
    public int addDigits(int num) {
        while(num>9)
        {
            int r=num%10;
            int s=num/10;
            int sum=r+s;
            num=sum;
        }
        if(num<=9)
        {
            return num;
        }
        return num;
    }
}
