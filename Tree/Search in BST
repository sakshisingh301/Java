functional code:
 boolean search(Node root, int x)
    {
	    if(root==null)
	    {
	    return false;
	    }
	    else if(x==root.data)
	    {
	        return true;
	    }
	    else if(x>root.data)
	    {
	        return search(root.right,x);
	        
	    }
	    else if(x<root.data)
	    {
	        return search(root.left,x);
	        
	    }
	    else
	    {
	        return false;
	    }
	     
	     
	     
	    
    }
